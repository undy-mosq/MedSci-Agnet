---
description: 生成 Vue 项目规范文档
alwaysApply: false
---

## 一、项目结构
```
src/
├── assets/          # 静态资源
├── components/      # 公共组件
│   ├── common/      # 基础 UI 组件
│   └── layout/      # 布局组件
├── composables/     # 组合式函数（useXxx）
├── directives/      # 自定义指令
├── router/          # 路由配置
├── store/           # Pinia 状态管理
├── styles/          # 全局样式
├── types/           # TypeScript 类型
├── utils/           # 工具函数
├── views/           # 页面组件（按模块）
├── App.vue
├── main.ts
└── env.d.ts
```

- 组件文件：`PascalCase.vue`
- 组合式函数：`camelCase.ts`（以 `use` 开头）
- 目录名：`kebab-case`

---

## 二、代码风格
- 缩进：2 空格
- 行尾分号：必须
- 引号：单引号
- 行宽：100 字符
- 使用 `<script setup lang="ts">`，结构顺序：导入 → props/emits → 响应式数据 → 计算属性 → 方法 → 生命周期 → 侦听器
- 样式默认 `scoped`，预处理器 SCSS

---

## 三、组件设计
- 基础组件（`common/`）无业务逻辑
- 业务组件按功能组织
- 页面组件负责组合与状态
- 通信：`defineProps` / `defineEmits`，跨级使用 `provide/inject` 或 Pinia
- 复用逻辑抽离为组合式函数

---

## 四、状态管理（Pinia）
- 使用 `defineStore`，setup 语法
- 按模块拆分 store，放在 `store/modules/`
- 组件中使用 `storeToRefs` 解构响应式属性

---

## 五、路由
- 路由懒加载：`() => import('@/views/...')`
- 路由 name：`PascalCase`，路径：`kebab-case`
- 全局守卫在 `router/index.ts`

---

## 六、API 请求
- Axios 封装（`utils/request.ts`），配置拦截器
- 按模块定义 API 函数，返回 Promise
- 统一错误处理，业务错误调用处 try/catch

---

## 七、TypeScript
- `strict: true`，显式声明类型
- 使用 `interface` 定义对象，`type` 用于联合等
- 避免 `any`，必要时用 `unknown`

---

## 八、样式
- SCSS 全局变量在 `styles/variables.scss`
- 类名 `kebab-case`，推荐 BEM

---

## 九、性能与安全
- 路由懒加载，列表渲染提供 `key`
- 避免 `v-html`，必须使用时过滤（DOMPurify）
- 敏感信息存储于 httpOnly cookie 或加密存储

---

## 十、测试
- 单元测试：Vitest
- 组件测试：Vue Test Utils
- 文件命名：`*.spec.ts`

---

## 十一、Git
- 分支：`main`、`develop`、`feature/*`、`fix/*`
- 提交：Conventional Commits（`feat:`、`fix:` 等）
- 提交前：Husky + lint-staged 运行 ESLint、Prettier、类型检查

---

## 十二、文档
- 组件和复杂逻辑提供注释 / JSDoc
- 项目根目录 `README.md`

---

## 十三、环境变量
- 文件：`.env.development`、`.env.production`
- 变量名以 `VITE_` 开头，通过 `import.meta.env` 访问

---

## 十四、构建与部署
- 构建命令：`npm run build`，输出 `dist/`
- 配置别名 `@` 指向 `src`

---

## 十五、AI 生成代码约束
- 禁止不安全代码（`eval`、未过滤的 `v-html` 等）
- 避免引入冗余依赖
- 确保响应式正确（ref/reactive）
- 保持可读性，适当拆分逻辑
