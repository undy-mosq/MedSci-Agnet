---
description: 生成Python文件规范
alwaysApply: false
---
# 生成Python文件规范


## 2. 代码风格
- 严格遵守 **PEP 8** 编码规范。
- 使用 **4个空格** 缩进，禁止使用Tab。
- 每行最大长度 **不超过88个字符**（推荐使用`black`格式化工具）。
- 在二元运算符（如`=`、`+`、`-`）前后各加一个空格；逗号、冒号后加空格，但括号内不加。
- 避免行尾多余空格。
- 使用`black`或`autopep8`自动格式化生成的代码。

## 3. 文件结构与模块组织
- 每个文件应包含一个清晰的模块级文档字符串（docstring），简要描述模块用途。
- 导入语句按以下顺序分组，各组之间空一行：
  1. 标准库导入
  2. 第三方库导入
  3. 本地应用/模块导入
- 禁止使用`from module import *`，应显式导入所需名称。
- 若文件可作为脚本执行，应包含`if __name__ == "__main__":`入口，并将主要逻辑封装在函数中。

## 4. 命名规范
| 类型          | 规范                | 示例                  |
|---------------|---------------------|-----------------------|
| 变量、函数    | 小写字母，单词间用下划线分隔 | `user_name`, `get_data()` |
| 类            | 驼峰命名法，首字母大写 | `UserProfile`, `DataLoader` |
| 常量          | 全部大写，单词间用下划线 | `MAX_RETRIES`, `DEFAULT_TIMEOUT` |
| 私有属性/方法 | 单下划线开头         | `_internal_state`, `_update_cache()` |
| 模块          | 小写字母，下划线可选 | `file_utils.py`       |
- 名称应具有描述性，避免使用单字母名称（循环变量除外，如`i`、`j`）。

## 5. 注释与文档字符串
- 使用 **中文** 注释，但整个项目内保持一致。
- 所有公共模块、类、函数、方法必须包含文档字符串（docstring），采用 **Google风格** ，并遵循 **PEP 257**。
- 文档字符串应清晰说明功能、参数、返回值、异常等。
- 复杂逻辑处添加行内注释，解释“为什么”而非“做什么”。

**示例（Google风格）**：
```python
def fetch_data(url: str, timeout: int = 10) -> dict:
    """从指定URL获取数据并返回解析后的JSON字典。

    Args:
        url: 请求的目标URL。
        timeout: 请求超时时间（秒），默认10秒。

    Returns:
        包含响应数据的字典。

    Raises:
        requests.RequestException: 当网络请求失败时。
    """
    ...
```

## 6. 类型提示
- 所有函数和方法必须添加 **类型提示（Type Hints）**，使用`typing`模块中的类型（如`List`、`Dict`、`Optional`、`Union`）。
- 对于Python 3.10+，可使用内置类型（如`list`、`dict`）及`|`联合类型。
- 变量类型提示可选，但在复杂场景下推荐添加。
- 使用`mypy`进行静态类型检查，确保类型提示正确。

## 7. 错误处理
- 异常应具体指定，**禁止使用裸`except:`**。
- 捕获异常后应进行适当处理（记录日志、重试、转换异常等），不应静默忽略。
- 自定义异常应继承自`Exception`，并添加清晰的文档。
- 在可能失败的操作（如文件IO、网络请求）中使用`try/except`，并考虑使用`with`语句管理资源。

## 8. 日志记录
- 使用`logging`模块记录日志，**禁止使用`print()`进行调试或信息输出**。
- 日志级别合理选择：`DEBUG`、`INFO`、`WARNING`、`ERROR`、`CRITICAL`。
- 在模块级别获取logger：`logger = logging.getLogger(__name__)`。
- 日志消息应包含足够的上下文信息，便于问题定位。

## 9. 安全性
- 严格验证外部输入（用户输入、文件内容、网络数据），防止注入攻击（如SQL注入、命令注入）。
- **禁止使用`eval()`、`exec()`** 或动态执行来自不可信源的代码。
- 使用`subprocess`模块时，避免`shell=True`，并使用列表形式的命令参数。
- 处理敏感信息（如密码、密钥）时，使用环境变量或安全配置，避免硬编码。
- 文件操作时注意路径遍历风险，使用`os.path`或`pathlib`进行安全路径拼接。

## 10. 依赖管理
- 若生成的项目需要第三方依赖，应生成`requirements.txt`或`pyproject.toml`，明确版本号（如`requests>=2.28.0,<3.0.0`）。
- 避免引入不必要的依赖。
- 在文档中说明安装方法。

## 11. 测试
- 重要功能应包含单元测试，使用`pytest`框架。
- 测试文件命名以`test_`开头，测试函数以`test_`开头。
- 测试应覆盖正常路径、边界条件和异常情况。
- 使用`mock`模拟外部依赖，保持测试独立性。

## 12. 版本控制
- 生成的项目应包含`.gitignore`文件，忽略Python缓存（`__pycache__/`）、虚拟环境、IDE配置等常见忽略项。
- 若项目可独立运行，应包含`README.md`，说明项目用途、安装步骤、使用方法。

## 13. 性能考虑
- 避免不必要的循环嵌套，优先使用列表推导式、生成器表达式。
- 对大数据集处理时使用迭代器而非列表一次性加载。
- 使用`timeit`或性能分析工具（如`cProfile`）识别瓶颈。

## 14. 可维护性
- 函数应保持单一职责，每个函数不超过50行（建议30行以内）。
- 类应遵循单一职责原则，避免上帝类。
- 适当使用设计模式（如工厂、策略）简化复杂逻辑。
- 代码应自解释，避免过度设计。

## 15. 示例
以下为一个符合规范的简短示例：

```python
"""提供用户数据获取与处理的模块。"""

import logging
from typing import Optional, Dict

import requests

logger = logging.getLogger(__name__)


class UserService:
    """用户服务类，负责与用户API交互。"""

    def __init__(self, base_url: str, timeout: int = 30):
        """初始化用户服务。

        Args:
            base_url: API基础URL。
            timeout: 请求超时时间（秒）。
        """
        self.base_url = base_url.rstrip('/')
        self.timeout = timeout

    def get_user(self, user_id: int) -> Optional[Dict[str, object]]:
        """根据用户ID获取用户信息。

        Args:
            user_id: 用户唯一标识。

        Returns:
            用户信息字典，若用户不存在则返回None。

        Raises:
            requests.RequestException: 当网络请求失败时。
        """
        url = f"{self.base_url}/users/{user_id}"
        try:
            response = requests.get(url, timeout=self.timeout)
            response.raise_for_status()
            return response.json()
        except requests.HTTPError as e:
            if e.response.status_code == 404:
                logger.warning(f"User {user_id} not found")
                return None
            logger.error(f"HTTP error fetching user {user_id}: {e}")
            raise
        except requests.RequestException as e:
            logger.error(f"Request error fetching user {user_id}: {e}")
            raise
```

